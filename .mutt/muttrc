# set date_format="%y-%m-%d %T"
# set index_format="%2C | %Z [%d] %-30.30F (%-4.4c) %s"

# bsd4chris account
set from = 'bsd4chris@gmail.com'
set mbox = "+bsd4chris"
set spoolfile = '+bsd4chris/INBOX'
set postponed ='+bsd4chris/[Google Mail]/Drafts'
set trash = 'bsd4chris/+[Google Mail]/Trash'

folder-hook . 'set read_inc=1000'

# Local folders for cached headers
set header_cache =~/.mutt/cache/headers
set message_cachedir =~/.mutt/cache/bodies

source ~/.mutt/colors.muttrc
# source ~/.mutt/colors2.muttrc

macro pager \cb <pipe-entry>'~/.mutt/scripts/urlview.sh'<enter> 'Follow links with urlview'
